<!DOCTYPE html>
<html lang="en">

<head>
  <title>Quiz Results</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/bootstrap/bootstrap.min.css" />
  <style>
    .result-header {
      background-color: #007bff;
      color: white;
      padding: 40px 0;
      text-align: center;
      position: relative;
    }

    .result-header h1 {
      margin-bottom: 10px;
      font-size: 2.5rem;
    }

    .result-header p {
      font-size: 1.2rem;
      margin-bottom: 0;
    }

    .back-to-app {
      position: absolute;
      top: 50%;
      right: 20px;
      transform: translateY(-50%);
    }

    .result-container {
      max-width: 600px;
      margin: 40px auto;
      text-align: center;
    }

    .result-item {
      padding: 20px;
      background-color: #f8f9fa;
      margin-bottom: 15px;
      border-radius: 5px;
      font-size: 1.25rem;
    }

    .result-item strong {
      display: block;
      font-size: 1.5rem;
    }
  </style>
</head>

<body>
  <!-- Header -->
  <header class="result-header">
    <div class="container">
      <h1>Quiz Results</h1>
      <p>Quiz: <%= quizTitle %></p>
      <a href="/app" class="btn btn-light back-to-app">Back to Dashboard</a>
    </div>
  </header>

  <!-- Main Content -->
  <div class="container result-container">
    <div class="result-item">
      <strong>Total Questions:</strong> <%= questionsCount %>
    </div>
    <div class="result-item">
      <strong>Correct Answers:</strong> <%= correctQuestions %>
    </div>
    <div class="result-item">
      <strong>Wrong Answers:</strong> <%= wrongQuestions %>
    </div>
    <div class="result-item">
      <strong>Final Score:</strong> <%= finalScore %> out of <%= maxScore %>
    </div>
    <div class="result-item">
      <strong>Final Percentage:</strong> <%= finalPercentage %>%
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>