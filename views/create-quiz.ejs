<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quiz Creation | Quizify | TUA</title>
  <link rel="stylesheet" href="/css/bootstrap/bootstrap.min.css" />

</head>

<body>
  <!-- Header Section -->
  <header class="bg-dark text-white py-3">
    <div class="container">
      <div class="d-flex justify-content-between align-items-center">
        <h1 class="h3">Create Quiz</h1>
        <nav>
          <a href="/" class="btn btn-outline-light">Home</a>
          <a href="/api/auth/logout" class="btn btn-light">Logout</a>
        </nav>
      </div>
    </div>
  </header>

  <div class="container mt-5">
    <div class="row">
      <div class="col-md-8 offset-md-2">
        <h1 class="mb-4">Create a New Quiz</h1>
        <p class="lead">Design a quiz with multiple-choice, true/false, or 2-option questions.</p>

        <form id="quiz-form" action="/api/create-quiz" method="POST">
          <!-- Quiz Details -->
          <h3 class="mb-3">Quiz Details</h3>
          <div class="form-group mb-3">
            <label for="title" class="form-label">Title</label>
            <input type="text" class="form-control" id="title" name="title" required>
          </div>

          <div class="form-group mb-3">
            <label for="description" class="form-label">Description</label>
            <textarea class="form-control" id="description" name="description" rows="2"></textarea>
          </div>

          <div class="form-group mb-3">
            <label for="max-time" class="form-label">Max Time per Question (in seconds)</label>
            <input type="number" class="form-control" id="max-time" name="maxTime" required>
          </div>

          <div class="form-group mb-3">
            <label for="score" class="form-label">Score per Correct Answer</label>
            <input type="number" class="form-control" id="score" name="score" required>
          </div>

          <div class="form-group mb-4">
            <label for="negative-score" class="form-label">Negative Scoring for Wrong Answers</label>
            <input type="number" class="form-control" id="negative-score" name="negativeScore" step="0.01" required>
          </div>

          <!-- Quiz Questions -->
          <h3 class="mb-3">Quiz Questions</h3>
          <div id="quizContainer">
            <div class="card mb-4">
              <div class="card-body" id="quiz1">
                <h5 class="card-title">Question 1</h5>
                <div class="form-group mb-3">
                  <label for="question1" class="form-label">Question</label>
                  <input type="text" class="form-control" id="question1" name="quiz[0][question]" required>
                </div>
                <div class="form-group mb-3">
                  <label for="quizType1" class="form-label">Select Quiz Type</label>
                  <select class="form-select" id="quizType1" name="quiz[0][type]" onchange="showOptions(this, 1)">
                    <option value="4-options">4 Options</option>
                    <option value="2-options">2 Options</option>
                    <option value="true-false">True/False</option>
                  </select>
                </div>
                <div class="options" id="options1">
                  <!-- Options will be dynamically added here -->
                </div>
              </div>
            </div>
          </div>
          <button type="button" class="btn btn-secondary mt-2" onclick="addQuiz()">Add Another Question</button>
          <button type="submit" class="btn btn-primary mt-4">Submit Quiz</button>
        </form>
      </div>
    </div>
  </div>

  <script src="./js/create-quiz.js"></script>
  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
]
</html>