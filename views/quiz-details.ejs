<!DOCTYPE html>
<html lang="en">

<head>
  <title>Quiz Details | Quizify</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/bootstrap/bootstrap.min.css" />
</head>

<body>
  <!-- Header Section -->
  <header class="bg-dark text-white py-3">
    <div class="container">
      <div class="d-flex justify-content-between align-items-center">
        <h1 class="h3">Quiz Details!</h1>
        <nav>
          <a href="/" class="btn btn-outline-light">Home</a>
          <a href="/api/auth/logout" class="btn btn-light">Logout</a>
        </nav>
      </div>
    </div>
  </header>

  <div class="container mt-5">
    <!-- Quiz Title -->
    <div class="text-center mb-4">
      <h1><%= quiz.title %></h1>
      <p class="lead text-muted"><%= quiz.description %></p>
    </div>

    <!-- Quiz Information -->
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card mb-4">
          <div class="card-header bg-primary text-white">
            <h5>Quiz Information</h5>
          </div>
          <div class="card-body">
            <ul class="list-group list-group-flush">
              <li class="list-group-item"><strong>Max Time:</strong> <%= quiz.maxTime %> minutes</li>
              <li class="list-group-item"><strong>Score per Correct Answer:</strong> <%= quiz.score %></li>
              <li class="list-group-item"><strong>Negative Score per Wrong Answer:</strong> <%= quiz.negativeScore %></li>
              <li class="list-group-item"><strong>Number of Questions:</strong> <%= quiz.questions.length %></li>
            </ul>
          </div>
        </div>
      </div>
    </div>

    <!-- Participate Button -->
    <div class="text-center">
      <form action="/participate/<%= quiz.quiz_tag %>" method="get">
        <button type="submit" class="btn btn-primary btn-lg">Participate</button>
      </form>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>


</html>